get:
  tags:
    - Kudu Branch
  summary: Branch
  description: Get all branches in Kudu
  operationId: KuduBranch
  security:
    - api_key: []
    - basic_auth: []
  responses:
    "200":
      description: OK
      headers:
        X-Rate-Limit:
          description: calls per hour allowed by the user
          schema:
            type: integer
            format: int32
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/kudu/branch.yaml
    "400":
      description: Unauthorized
