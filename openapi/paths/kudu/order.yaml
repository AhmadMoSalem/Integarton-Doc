tags:
  - Kudu Order
summary: Order
description: Create Order in Kudu
operationId: KuduCreateOrder
security:
  - api_key: []
  - basic_auth: []
requestBody:
  content:
    application/json:
      schema:
        $ref: ../../components/schemas/kudu/order.yaml
  description: requestBody description
  required: true
responses:
  # "200":
  #   description: OK
  #   content:
  #     application/json:
  #       schema:
  #         $ref: ../../components/schemas/kudu/response/success.yaml
  "400":
    description: Error from Kudu
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/response/unauthorized.yaml
  "401":
    description: Unauthorized
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/response/unauthorized.yaml
