# post:
  tags:
    - Branch
  summary: Branch
  description: Get all branches in Foodics for specific restaurant
  operationId: FoodicsBranch
  security:
    - api_key: []
    - basic_auth: []
  responses:
    '200':
      description: OK
      headers:
        X-Rate-Limit:
          description: calls per hour allowed by the user
          schema:
            type: integer
            format: int32
      content:
        application/json:
          schema:
             $ref: ../components/schemas/foodics/branch.yaml
          examples:
            response:
              value: Hello world!
        application/xml:
          schema:
            type: string
        text/csv:
          schema:
            type: string
    '400': 
      description: Unauthorized

